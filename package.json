{
  "private": true,
  "scripts": {
    "start": "parcel index.html --out-dir dist --log-level 4",
    "build": "NODE_ENV=production parcel build index.html --out-dir dist"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-hot": "^1.0.1",
    "node-elm-compiler": "^5.0.1",
    "parcel-bundler": "^1.11.0"
  },
  "dependencies": {
    "firebase": "^5.7.1",
    "jquery": "^3.3.1",
    "semantic-ui-css": "^2.4.1"
  }
}
